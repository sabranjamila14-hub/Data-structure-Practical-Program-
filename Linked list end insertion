class Solution:
    def insertAtEnd(self, head, x):
        new_node = Node(x)

        # If list is empty
        if head is None:
            return new_node

        temp = head

        # Traverse to the last node
        while temp.next:
            temp = temp.next

        # Insert at end
        temp.next = new_node

        return head
